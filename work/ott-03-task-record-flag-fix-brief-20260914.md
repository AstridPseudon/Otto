You are the bounded OTT-03 correction worker. Work only in this checkout, from current HEAD ccec6edc059721bfcb9ea8e1e0a31215a777cb2. Do not edit the sealed v2/v3 evidence, accepted Herzchen source, or any other checkout.

Goal: correct the selected WorkTemplate pending response so its `task_created` field truthfully reflects the returned typed task records/task_refs. In `src/otto/portfolio/herzchen_binding.py`, the selected template pending return currently returns nonempty `tasks` and `task_refs` while hard-coding `task_created: False`. Change only this semantics to report `bool(task_records)` (or an equally precise presence-based value), while preserving separate `executable`, `activation`, `dispatch`, `budget_reserved`, and `session_created` flags as false. Blank/default and handoff paths with no task records must remain false. Do not weaken replay conflict, exact replay, invalid-resource, inertness, finite consumer, or owner/bootstrap behavior.

Update the narrow affected test expectation(s) to assert a typed task record is reported as created/present while dispatch, execution, budget, session-created-task remain false. Keep the test discriminating and verify exact replay still returns the task refs and zero new events. Do not add broad tests.

Validation: run the selected source tests with the existing venv and `PYTHONPATH=src`; then build a fresh Otto wheel from the corrected commit into a new versioned evidence directory and run the same selected tests installed with `PYTHONPATH` and `PYTHONHOME` unset, using the accepted Herzchen wheel SHA 2681daaeb673cd636050c3805977a01a670ed6bcfc113fe9d85903af5eeae9bc. Capture exact command, cwd, model/reasoning, timestamps, exit codes, source commit/tree, wheel SHA, and installed origins. Preserve prior 47/18 and observation evidence as historical; create a new supplement only. No remote writes, no review, no gate claim.

Commit product/tests separately if practical, then commit the new focused evidence and receipt. Return exact commits/trees/wheel SHA, changed paths, and test receipts. Requested route: gpt-5.6-luna, reasoning high.
