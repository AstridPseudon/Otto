# OTT-04 result — durable attention and continuity

Status: **bounded implementation delivered; OTT-04 is not complete**. The
owned adapter and tests are green, but the accepted Herzchen wheel does not
provide a finite public durable-host command for the complete Otto due record,
and no real plan/dispatch amendment port was supplied. Those are recorded as
unresolved shared-capability gaps below; no success is claimed for those
surfaces or for OTT-06.

## Custody and execution

- Route: `gpt-5.6-luna`, `model_reasoning_effort=high`, normal worker; no
  review/oracle/AST work.
- Launch custody: manager-corrected start `a5b9c5fad33f52bcd7f155cf84571c1c1a221953`
  / tree `14dc21521d3221d6ef8c6151848be8de9f83154b`, with custody ancestor
  `0806be099eecdc9b861677ef7e71f7b54d2789a6` / tree
  `3527a4dd83f32f23bd36fabce591f81fd12e50ec`. The manifest base remains the
  manager-owned `1abfa7ffcebf6bc356545167c7dd8dade86b042b` / tree
  `7dc5499e3f1577695019a18dfd793bef0f43233d`; the corrected launch receipt
  records the later accepted-input correction.
- Product source: `aea1ac6471784256a408daaef324dab9f5cba571` /
  `0f2246eefb5e5996355a8c31d3cc7a8f76655875`.
- Accepted Herzchen source: `b68d8f59712275229c24757664ca30d807d39e4a` /
  `ee1614fc37fdd096fb9640702e124e28a391d170`.
- Accepted Herzchen wheel SHA-256:
  `2681daaeb673cd636050c3805977a01a670ed6bcfc113fe9d85903af5eeae9bc`.
- Fresh Otto wheel SHA-256:
  `dc9f51338119bd4ff91caef1f535b0f5a4145177cdefcbced94d4100eb6a5a87`.
- P01 profile SHA-256 verified unchanged:
  `9d277b810adcbeac445d5aff2fc31a8052cfa99b844696e11412a90d11744726`.
- Owned source SHA-256: `src/otto/attention.py` =
  `e5ef398185242a833f4f2a5d5d66648f86828a815f8015a2fe7993268c5c0c8d`;
  `tests/otto/test_continuity.py` =
  `836892f96b5ce72e12911282b44e47fca8514d6c0544ae785d9ebdeadabfbe13`.
- Manager process/session receipts: prior stale launch was recorded as
  aborted before substantive work in
  `work/ott-04-preflight-aborted-20260914/launch-receipt.json`; corrected
  launch resumed the same thread with PID `68660` in
  `work/ott-04-launch-corrected-20260914/launch-receipt.json`. The final
  source/installed same-process campaign PIDs were `70061` and `70056`.

## Proof results

The required representative path ran before broad propagation against the
fresh installed Otto wheel plus the accepted Herzchen wheel, with
`PYTHONPATH` and `PYTHONHOME` unset. A real disposable Store and
`ContextPacketService.notify_amendment` produced a committed attention event
and receipt. Otto passed only finite JSON-shaped calls; the boundary had only
`_create`/`_read` and no Store field. `ContextPacketService.list_attention`
reopened one open attention after Store close/reopen, and the shared
`EventCursorReader` returned the attention event without acknowledging it.

Observation artifact:
`work/ott-04-real-milestone-20260914/representative-observation-final-v2.json`
(SHA-256 `bdf566ab219e3e69649a188e12f84fa354077738d4f138e935781431517d37af`).
It records two persisted events, one real attention receipt/event ID, the
attention stream and authority, `acknowledged: false`, and the reopened open
attention.

Behavior covered by the owned tests:

- one-off due checks and three-hour plus 90-minute interval checks use one
  deterministic injected clock; missed slots coalesce, anchors survive a
  durable-port restart, and one request remains in flight;
- exact request completion, replay, changed-input rejection, stop state,
  crash/unknown reconciliation, same-request resume, owner, missing handoff,
  return condition, and truthful unavailable-host results;
- cursor duplicate-read, gap, out-of-order/error classification, expiry and
  recoverability; reading is not acknowledgment;
- readiness is attention-only (`dispatch: false`, `executable: false`);
  manager `wait` is recorded without manager/task/budget creation;
- review-handled remains distinct from implemented improvement; the finite
  amendment adapter checks plan, next dispatch, attention and assignment view
  changes, preserves owner/usage/running input pins, and reports incomplete
  propagation.

The interval, durable-host, amendment and non-normal cursor cases are
fixture/adapter proofs. The real product proof is the installed Store,
packet/attention, event-cursor, receipt, and reopen observation above.

## Campaign receipts

- Source same-process campaign: `53 passed`, return code `0`,
  `work/ott-04-real-milestone-20260914/source-same-process-campaign.txt`.
  Otto origin was `src/otto`; Herzchen origin was the accepted wheel. This
  source campaign intentionally used `PYTHONPATH=src`.
- Installed same-process campaign: `53 passed`, return code `0`,
  `work/ott-04-real-milestone-20260914/installed-same-process-campaign.txt`.
  Both origins were under the disposable venv `site-packages`, with
  `PYTHONPATH=None` and `PYTHONHOME=None` in that process.
- Focused source and installed OTT-04 tests: `6 passed`, return code `0` in
  `source-continuity-final-v5.txt` and `installed-continuity-final-v5.txt`.
- Exact build/install command outputs are preserved under
  `work/ott-04-real-milestone-20260914/` (`build-otto-wheel-release.txt`,
  `install-otto-release.txt`, and the origin/campaign receipts).

## Unresolved gaps and handoff

1. `IntervalController` in the accepted wheel persists only interval seconds,
   anchor, last slot and in-flight request. There is no accepted finite public
   Herzchen endpoint that persists Otto's complete recipient/instruction/profile/
   invocation/stop/outstanding-decision due record. `DueRecordPort` is therefore
   an explicit trusted-host boundary, and its durable-host behavior is proven
   only with a fixture implementation. The awaited real product mode is
   proven and truthfully reports `durable: false`.
2. The accepted public packet attention API proves amendment notifications, but
   no accepted plan/next-dispatch/current-assignment propagation endpoint was
   injected. Improvement propagation is consequently adapter/fixture evidence,
   not product proof; a manager must route this gap before claiming completion.
3. No real agent host launch, crash reconciliation, or later OTT-06 rehearsal
   was performed. The implementation returns the original invocation identity
   and `relaunch: false` for unknown/crashed delivery, but this is not a claim
   of a qualified host transport.

No Herzchen, portfolio, profile, package/control, or forbidden manager test
path was edited. The manager-generated launch/preflight receipt directories
remain separate from this owned increment.
