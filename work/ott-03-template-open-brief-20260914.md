# OTT-03 selected-template create-and-open continuation

You are the bounded owner-bridge implementation worker. Work only in the isolated Otto checkout assigned at launch. The accepted baseline is source/evidence commit `74645e6aeb0f9dbd9d992aefc542c91e80624f94` (source parent `3237701d21470380cbe471880d793449b5f40d9b`, tree `669d3af0e65bea36f4349f6374c4d7073f3a605a`). Preserve all prior source and evidence commits and do not edit the sealed v2 evidence directory. The accepted Herzchen source and wheel remain immutable: source `b68d8f59712275229c24757664ca30d807d39e4a`, wheel SHA `2681daaeb673cd636050c3805977a01a670ed6bcfc113fe9d85903af5eeae9bc`.

Implement the remaining original OTT-03 requirement: a real selected `WorkTemplate` create-and-open path through Otto's trusted owner bridge and accepted public Herzchen APIs. Use the current `FiniteWorkOperations._create_and_open` and `PortfolioOwnerBootstrap` bridge. Add a typed, finite request field for the validated JSON template resource; keep the consumer boundary free of Store, DB descriptors, SQL, generic writers, callbacks, pickle, or arbitrary callable transport. The owner-only callback may construct the accepted `WorkTemplate`, validate/render it with public helpers, and invoke the accepted owner `ProjectSheet.command_port.instantiate_new_template` for the reserved project. Do not modify Herzchen source.

The operation must preserve occupation-before-creation and the existing trusted AuthoringSessionService create-and-open lifecycle. A valid selected template must create one durable pending project, apply its template-defined fields and typed tasks in the owner materialization transaction, return the real authoring handle/scope/session and linked receipts, and remain inert (no dispatch, manager launch, budget, session-created task, or execution). The selected template request/resource and parameters must participate in canonical same-key digest. Exact same-key replay returns the original project/open result with no new events; changed template/resource/parameter input conflicts with zero durable delta; invalid resource/seed/parameters fails before project creation or leaves only the existing truthful saved-project recovery state. Fresh close/reopen reads the selected fields/tasks and authoring scope. Repeat occupied/open and materialization failure must preserve existing v2 semantics. Blank/default create-and-open must remain fully regression-tested.

Do not weaken or rewrite the already accepted v2 tests. Add focused tests in the Otto-owned tests path for selected create-and-open, real owner bridge callback, typed task/content, replay/conflict/invalid resource, fresh reopen, inertness, occupation and blank regression. Use the accepted installed Herzchen wheel and public ports only. Run source and installed focused campaigns with `PYTHONPATH` and `PYTHONHOME` unset for installed imports, record exact argv/start/end/return code/model/reasoning/cwd/write mode/origins, and build a fresh Otto wheel. Include the real selected create-and-open observation JSON and an explicit capability/coverage matrix showing the formerly pending row is now supported. Preserve the prior selected-template amendment evidence as historical and create a new versioned evidence directory.

Use normal owner-side trusted composition only; do not expand to OTT-06, G-OTTO, EX-HOST, live Astrid, or programme claims. If the accepted public owner APIs genuinely cannot perform the typed operation, first provide a concrete source-level failure and exact endpoint requirement to the manager; do not silently label the row accepted. Commit product/tests separately from evidence. At completion report exact source/evidence commits and trees, wheel SHA, test counts, raw observations, and any unresolved named issue.
